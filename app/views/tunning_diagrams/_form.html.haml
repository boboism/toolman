= simple_form_for(@tunning_diagram, html: {class: "well form-horizontal"}) do |f|
  = f.error_notification
  = display_base_errors @tunning_diagram
  .form-inputs
    = f.input :archive

  .form-actions
    = f.button :submit, t('helpers.links.submit'), class: 'btn-primary'
